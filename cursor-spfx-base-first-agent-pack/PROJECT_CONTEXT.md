# Project Context

This file is for project-specific facts only. Keep it short and factual.

## System

- System name:
- Repo type: SharePoint Framework (SPFx)
- UI stack:
- State management:
- SharePoint access method: REST / PnPjs / Graph / custom API / mixed
- Testing tools:
- Build command:
- Serve command:
- Package command:

## Existing architecture notes

Document what already exists in the codebase:

- Main web parts/extensions:
- Main folders and responsibilities:
- Existing service/repository patterns:
- Existing component patterns:
- Existing error handling pattern:
- Existing logging pattern:
- Existing permission pattern:

## Non-negotiables

- Do not invent business rules.
- Do not invent SharePoint lists, columns, content types, APIs, permissions, or workflows.
- Use only contracts and examples that already exist in the repo or are explicitly provided by the user.

## User-provided business/data model

Add business rules and data model here only after the user provides them.
